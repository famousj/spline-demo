import React, { useState } from 'react';

interface SplinePoint {
    x: number;
    y: number;
}

const Splines: React.FC = () => {
    const [points, setPoints] = useState<SplinePoint[]>([]);


    return (
        <>
        </>
    );
};
